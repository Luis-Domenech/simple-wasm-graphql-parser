{
  "private": true,
  "workspaces": [
    "wasm-graphql-parser-src",
    "wasm-graphql-parser-src/pkg",
    "packages/backend-test"
  ],
  "devDependencies": {
    "@commitlint/cli": "16.2.3",
    "@commitlint/config-conventional": "16.0.0",
    "husky": "7.0.4"
  },
  "scripts": {
    "prepare": "husky install",
    "test": "cd wasm-graphql-parser-src && yarn compile-dev && cd ../packages/backend-test && yarn test",
    "deploy": "cd wasm-graphql-parser-src && yarn deploy"
  }
}