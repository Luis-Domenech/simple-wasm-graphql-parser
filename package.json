{
  "private": true,
  "workspaces": [
    "packages/*",
    "tests/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "16.2.3",
    "@commitlint/config-conventional": "16.0.0",
    "husky": "7.0.4"
  },
  "scripts": {
    "prepare": "husky install",
    "test": "cd src && yarn compile-dev && cd ../packages/backend-test && yarn test"
  }
}