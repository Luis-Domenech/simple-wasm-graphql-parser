{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "16.2.3",
    "@commitlint/config-conventional": "16.0.0",
    "husky": "7.0.4"
  },
  "scripts": {
    "prepare": "husky install",
    "test–pkg": "cd wasm-graphql-parser-src && yarn compile-dev && cd ../packages/backend-test && yarn test",
    "publish-pkg": "cd wasm-graphql-parser-src && yarn publish-pkg"
  }
}