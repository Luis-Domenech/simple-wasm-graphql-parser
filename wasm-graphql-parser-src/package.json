{
  "name": "wasm-graphql-parser",
  "description": "Just a basic graphql parser that uses Apollo's rust parser and custom logic to extract all info from a schema",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "cp-types": "cat ./typescript-types.ts >> ../packages/wasm-graphql-parser/wasm_graphql_parser.d.ts",
    "gen-npmrc": "ts-node ./gen-npmrc.ts",
    "test": "cargo test",
    "check": "cargo check",
    "build": "cargo build",
    "compile-dev-node": "rimraf ../packages/wasm-graphql-parser && wasm-pack build --target nodejs --out-dir ../packages/wasm-graphql-parser -- --offline && yarn cp-types",
    "compile-dev-bundler": "rimraf ../packages/wasm-graphql-parser && wasm-pack build --target bundler --out-dir ../packages/wasm-graphql-parser -- --offline && yarn cp-types",
    "compile-dev-web": "rimraf ../packages/wasm-graphql-parser && wasm-pack build --target web --out-dir ../packages/wasm-graphql-parser -- --offline && yarn cp-types",
    "compile-dev": "yarn && yarn compile-dev-node",
    "compile-prod-node": "rimraf ../packages/wasm-graphql-parser && wasm-pack build --target nodejs --out-dir ../packages/wasm-graphql-parser --release -- --offline",
    "compile-prod-bundler": "rimraf ../packages/wasm-graphql-parser && wasm-pack build --target bundler --out-dir ../packages/wasm-graphql-parser --release -- --offline",
    "compile-prod-web": "rimraf ../packages/wasm-graphql-parser && wasm-pack build --target web --out-dir ../packages/wasm-graphql-parser --release -- --offline",
    "compile-prod": "yarn && yarn compile-prod-node && yarn cp-types && yarn gen-npmrc",
    "deploy": "yarn && yarn compile-prod && wasm-pack pack ../packages/wasm-graphql-parser"
  },
  "dependencies": {},
  "author": "Luis F. Domenech Ortiz <luisfabiandomenech@gmail.com>",
  "homepage": "https://github.com/Luis-Domenech/wasm-graphql-parser",
  "repository": {
    "type": "git",
    "url": "https://github.com/Luis-Domenech/wasm-graphql-parser"
  },
  "keywords": [
    "rust",
    "web assembly",
    "wasm",
    "graphql",
    "parser",
    "schema",
    "typescript"
  ],
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
